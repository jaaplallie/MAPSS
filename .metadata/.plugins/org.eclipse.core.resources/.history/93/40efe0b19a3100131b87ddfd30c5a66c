package Backend;

import org.jfree.chart.*;
import org.jfree.data.general.*;
import org.jfree.data.xy.DefaultXYDataset;

public class graphicDraw {

	DefaultPieDataset data = new DefaultPieDataset();
	DefaultValueDataset data2 = new DefaultValueDataset();
	JFreeChart chart;
	JFreeChart chart2;
	ChartFrame frame = new ChartFrame("Test", chart);
	ChartFrame frame2 = new ChartFrame("Test", chart2);
	
	public graphicDraw() {
	}
	
	
	public void drawGraphic(){
		// create a dataset...
		DefaultPieDataset data = new DefaultPieDataset();
		DefaultValueDataset data2 = new DefaultValueDataset();
		
		data.setValue("Category 1", 250);
		data.setValue("Category 2", 500);
		data.setValue("Category 3", 310);
		data2.setValue(250);
		
		// create a chart...
		JFreeChart chart = ChartFactory.createPieChart(
		"Sample Pie Chart",
		data,
		true, // legend?
		true, // tooltips?
		false // URLs?
		);
		
		JFreeChart chart2 = ChartFactory.createPieChart(
				"Sample Chart",
				data,
				true, // legend?
				true, // tooltips?
				false // URLs?
				);
		
		// create and display a frame...
		ChartFrame frame = new ChartFrame("First", chart);
		frame.pack();
		frame.setVisible(true);
		
		ChartFrame frame2 = new ChartFrame("second", chart2);
		frame2.pack();
		frame2.setVisible(true);
	}
	
}
